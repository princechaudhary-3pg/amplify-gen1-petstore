type Pet @model @auth(rules: [{allow: public, operations: [read, create, update]}, {allow: owner}]) {
  id: ID!
  name: String!
  age: Int
  breed: String
  about: String
  image: String
  color: String
}
 